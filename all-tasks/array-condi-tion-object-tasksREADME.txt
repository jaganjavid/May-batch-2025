# JavaScript Assignment Tasks

Welcome students! 🎓

This assignment covers **Arrays**, **Objects**, and **Conditional Statements** in JavaScript. Each topic contains 10 creative and practical tasks designed to build your foundational and intermediate skills. Please solve each task in a separate `.js` file and submit your solutions.

---

## 📌 Array Tasks
1. Create an array of 7 days and print the third day.
2. Write a function to double each number in an array and return a new array.
3. Find the maximum number in an array without using Math.max().
4. Combine two arrays of fruits and remove duplicates.
5. Remove the first 3 elements from an array using `.splice()`.
6. Add an item to both the start and end of an array using array methods.
7. Count how many times a number appears in an array.
8. Reverse an array and log it as a comma-separated string.
9. Write a function to return only even numbers from an array.
10. Rotate the elements of an array to the right by 1 position.

---

## 📌 Object Tasks
1. Create a `book` object with `title`, `author`, and `price` properties.
2. Add a method in an object to return its formatted description.
3. Write a function to count all keys in a given object.
4. Check if a given key exists in an object and return a message.
5. Clone an object using the spread operator and modify one key.
6. Create a nested object structure to represent a student's course and marks.
7. Loop through the values of an object and print them.
8. Convert an object to an array of `[key, value]` pairs.
9. Write a function to deeply compare two objects and return true if equal.
10. Create an object that stores functions as values and call one of them.

---

## 📌 Condition Tasks
1. Check if a number is odd or even.
2. Write a function that checks if a person is eligible to vote (age ≥ 18).
3. Create a program to check if a variable is undefined.
4. Compare two strings using strict equality.
5. Use an `else-if` ladder to categorize temperature ranges (cold, warm, hot).
6. Use `&&` to check if a user is both logged in and has admin access.
7. Use `||` to determine if someone is a student or senior for a discount.
8. Use a `switch` statement to return the season based on month number.
9. Check if a number is divisible by both 3 and 5.
10. Write a function that takes a score and returns a grade (A-F).

---

Happy Coding! 💻🔥
